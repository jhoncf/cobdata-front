services:
  web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: /api
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "127.0.0.1:18110:80"
